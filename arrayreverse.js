

function reverseArray(arrayToReverse) {
   let arrayReversed = [...arrayToReverse];

   while (array.length > 0){
   arrayReversed.push( array.pop());
   }
   return arrayReversed;
}

reverseArray(myArray);